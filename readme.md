# Kivy Calculator (Android)

Simple calculator app built with **Python + Kivy** and compiled for Android using **GitHub Actions**.

## How to build the APK

Just push to the `main` branch.

GitHub Actions will automatically:

✔ build the APK  
✔ store it as an artifact  
✔ allow download from the Actions tab

## Run locally

